#include "main.h"

/**
 * _abs - a function to turn  a number to his absolute value
 * @n: number to change
 *
 * Return: absolute value
 */
int _abs(int n)
{
	if (n < 0)
		return (-n);
	return (n);
}
