# include "mqin.h"
/**
 * add - a function that add two number
 * @a: a parametre given to add
 * @b: anoter parametre to add
 *
 * Return: return sum of the two numbers
 */
int add(int a, int b)
{
	int sum = 0;

	sum = a + b;
	return (sum);
}

